name: Build and Deploy to VPS (Develop)

on:
  push:
    branches:
      - develop

jobs:
  build:
    name: Build backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # On reprend ton étape de build originale, qui fonctionnait
      - name: Create archive
        run: |
          set -euxo pipefail
          rm -rf dist
          mkdir -p dist
          rsync -a --delete \
            --exclude='.git' \
            --exclude='node_modules' \
            --exclude='.env' \
            --exclude='.DS_Store' \
            --exclude='dist' \
            ./ dist/
          # On crée l'archive à partir du dossier dist
          tar -czf snoroc_back.tar.gz -C dist .
          ls -lh snoroc_back.tar.gz

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: snoroc_back
          path: snoroc_back.tar.gz
          if-no-files-found: error

  deploy:
    name: Deploy backend to VPS
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: snoroc_back
          path: .

      - name: Copy archive to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }} # Doit être 'ubuntu'
          key: ${{ secrets.SSH_KEY }}
          source: 'snoroc_back.tar.gz'
          target: '/tmp'

      # Cette étape contient le script corrigé pour le VPS
      - name: Deploy and restart backend
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }} # Doit être 'ubuntu'
          key: ${{ secrets.SSH_KEY }}
          script: |
            set -euxo pipefail
            echo "🚀 Starting deployment on VPS..."

            # Le bon chemin vers ton projet sur le VPS
            PROJECT_DIR="/home/ubuntu/snoroc-dev/snoroc_back"

            mkdir -p "$PROJECT_DIR"

            # Backup .env
            tmp_env=""
            if [ -f "$PROJECT_DIR/.env" ]; then
              tmp_env=$(mktemp)
              cp "$PROJECT_DIR/.env" "$tmp_env"
              echo "File .env backed up."
            fi

            echo "Cleaning old application files (except .env)..."
            find "$PROJECT_DIR" -mindepth 1 -maxdepth 1 ! -name '.env' -exec rm -rf {} +

            echo "Extracting new application files..."
            tar -xzf /tmp/snoroc_back.tar.gz -C "$PROJECT_DIR"

            # Restore .env
            if [ -n "${tmp_env:-}" ] && [ -f "$tmp_env" ]; then
              mv "$tmp_env" "$PROJECT_DIR/.env"
              echo "File .env restored."
            fi

            rm -f /tmp/snoroc_back.tar.gz

            # Aller dans le dossier du projet pour lancer docker compose
            cd "$PROJECT_DIR"

            echo "Building and restarting Docker containers..."
            sudo docker compose -f docker-compose.yaml up -d --build api

            echo "✅ Backend deployed and restarted successfully"

      - name: Verify API response
        run: |
          echo "🌐 Checking API response..."
          sleep 5
          curl -s http://${{ secrets.SSH_HOST }}:3030 || echo "⚠️ API check failed"
